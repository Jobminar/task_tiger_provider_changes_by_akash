import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tasktigersprovider.app',
  appName: 'coolie-no.1-provider',
  webDir: 'dist'
};

export default config;
